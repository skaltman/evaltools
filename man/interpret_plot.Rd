% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-in-middle.R
\name{interpret_plot}
\alias{interpret_plot}
\title{Interpret a plot using a fresh model context}
\usage{
interpret_plot(plot_file, solver_chat, prompt_file = NULL)
}
\arguments{
\item{plot_file}{Path to a plot image file (PNG, JPG, etc.)}

\item{solver_chat}{An ellmer Chat object to clone for fresh context}

\item{prompt_file}{Optional path to a custom interpretation prompt. If NULL,
uses the default prompt from inst/prompts/interpret_plot.md}
}
\value{
An ellmer ContentToolResult with formatted text description of the plot
}
\description{
Uses a "model-in-the-middle" approach where a fresh chat context (cloned from
the solver_chat) interprets a plot image and returns a text description instead
of the image itself. This helps isolate visual interpretation from contextual
priors and expectations.
}
\details{
The interpretation includes:
\itemize{
\item Axis ranges
\item Distribution and shape of plotted data
\item Additional relevant context (facets, anomalies, etc.)
\item A reflection prompt asking the solver to note expectation alignment
}
}
\examples{
\dontrun{
chat <- ellmer::chat("anthropic/claude-sonnet-4-5-20250929")
result <- interpret_plot("plot.png", chat)
}
}
